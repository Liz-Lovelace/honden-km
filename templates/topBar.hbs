<div class="top-bar">
  {{> search this}}

  <div>
    <button hx-delete="/inode?uuid={{inode.uuid}}" hx-confirm="Are you sure you want to delete"> DELETE </button>

     
    {{#isType inode 'note'}}
      <button hx-put="/edit_note?uuid={{inode.uuid}}"> EDIT CONTENT </button>
    {{/isType}}

    <form hx-patch="/rename?uuid={{inode.uuid}}">
      <input name="newName" type="text" id="newFilename" placeholder="rename">
    </form>
  </div>

  <div>
    <button hx-post="/note" hx-prompt="Enter a name for the new note"> New note </button>
    <button> <s> 十 Media </s> </button>
  </div>
</div>

<style>
  .top-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 10px;
  }

  .top-bar div {
    display: flex;
    gap: 10px;
    align-items: center;
  }
</style>